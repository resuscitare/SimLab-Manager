import Papa from 'papaparse';  // Agora instalado
import * as XLSX from 'xlsx';  // Agora instalado
import { isPast, differenceInDays } from 'date-fns';  // Agora instalado

// Resto do código do componente...
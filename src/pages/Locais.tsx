import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";  // Agora instalado
import { showError, showSuccess } from "@/utils/toast";  // Agora instalado

// Resto do código do componente...
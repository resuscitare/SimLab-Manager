import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command";  // Agora instalado
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";  // Agora instalado

// Resto do código do componente...